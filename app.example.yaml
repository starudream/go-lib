log:
  console:
    disabled: false
    format: text
    level: DEBUG
  file:
    enabled: true
    format: json
    level: FATAL
    max_size: 100
    max_age: 0
    max_backups: 10
    daily_rotate: true

ntfy:
  timeout: 3s
  proxy:
  retry: 1

  dingtalk:
    token:
    secret:

  telegram:
    token:
    chat_id:

  webhook:
    url:
    method:
    type:
    key:
    extra:
    headers:

jwt:
  expire_time: 360h
